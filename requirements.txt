# Pinned runtime dependencies for a reproducible environment.
#
# These are the exact versions the analysis was run and tested with. For an
# editable install with looser (>=) bounds suitable for library reuse, see
# pyproject.toml and run:  pip install -e .
#
# `pyinform` is optional (a pure-NumPy transfer-entropy fallback ships with the
# package); install it for exact parity with the paper's estimator.
numpy==2.3.5
pandas==2.3.3
scipy==1.16.3
statsmodels==0.14.5
pingouin==0.6.1
scikit-learn==1.6.1
matplotlib==3.10.6
seaborn==0.13.2
pyarrow==21.0.0
tqdm==4.67.1
pyyaml==6.0.3
# Optional:
# pyinform==0.2.0
